= render 'shared/header_nav'
= render 'shared/carousel'
= render 'shared/navigation'
.container
  = render 'shared/hero'
  / = render 'shared/four_by_two_snippet_template'

= render_region 'details', 'full', {}

= render 'shared/footer'

